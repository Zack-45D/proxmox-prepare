[Unit]
Description=SMARTctl exporter for Prometheus
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
ExecStart={{ smartctl_exporter_binary_path }} --web.listen-address=:{{ smartctl_exporter_port }}
Restart=on-failure
RestartSec=5

[Install]
WantedBy=multi-user.target